# include <stdio.h>
# include <string.h>


int main() {
	
	char  palavra[101];
	int   i, tamanho;
	fflush(stdin);
	while (gets(palavra) != EOF ) {
		
		tamanho = strlen(palavra);
		for (i = 0; i < tamanho; i++) {
			
			
		}
		
		
		
	
	
	
	
	
	}
	// apenas para verificação
	printf("\nA palavra lida é: %s", palavra);
}
